/**
 * Returns a timeout value in milliseconds based on the input number.
 * The timeout is calculated as `n * 1000` milliseconds.
 *
 * @param n - The input number.
 * @returns The timeout value in milliseconds.
 */
export const getComplexityTimeout = (n) => {
    return n * 1000;
};
